# Registry URL Configuration
# The registry is always served by the Next.js app

# Auto-detection (no configuration needed):
# 1. localhost:3000 for local development
# 2. window.location.origin for browser (same domain as the app)
# 3. Environment-based Vercel URLs:
#    - Production: VERCEL_PROJECT_PRODUCTION_URL (if VERCEL_ENV=production)
#    - Preview: VERCEL_URL (if VERCEL_ENV=preview)
#    - Fallback: VERCEL_URL (for backward compatibility)
# 4. https://domain-ui.dev as fallback

# Vercel Environment Variables (auto-provided by Vercel):
# VERCEL_ENV=production|preview|development
# VERCEL_URL=your-deployment-abc123.vercel.app
# VERCEL_PROJECT_PRODUCTION_URL=your-custom-domain.com

# Registry paths:
# - /r/{component}.json (free registry)
# - /r-pro/{component}.json (pro registry)